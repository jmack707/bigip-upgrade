# inventory/hosts.ini
# Primary devices are upgraded first; secondary devices follow.
# With serial: 1 in the playbook, Ansible processes one host at a time —
# primary completes and is verified healthy before secondary begins.

[bigip_primary]
172.16.1.245

[bigip_secondary]
# 10.1.1.5

# Combined group — target of the playbook (hosts: bigip)
[bigip:children]
bigip_primary
bigip_secondary
