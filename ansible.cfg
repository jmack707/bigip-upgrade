[defaults]
roles_path          = roles
inventory           = inventory/hosts.ini
interpreter_python  = auto_silent
nocows              = true
retry_files_enabled = false
# Human-readable task results (Ansible 2.13+)
callback_result_format = yaml
