---
# vars/main.yml
# ─────────────────────────────────────────────────────────────────────────────
# Role-internal constants. Do not edit.
# Operator variables (ISO image, target version) live in vars/bigip_upgrade_vars.yml.
# Tunable defaults (validate_certs, timeouts, debug) live in defaults/main.yml.
# ─────────────────────────────────────────────────────────────────────────────

# Volume name prefix — informational reference only.
# Actual target volume is selected dynamically in tasks/main.yml.
software_volume_prefix: "HD1."
